"""The review web application.

One package, `app`.  There were four -- `inputs`, `consensus`, `run_report` and
`etl_review` -- on four ports, with four navigation bars, 47 shared templates
and no links between them.  They are gone; see `plans/webapp-consolidation.md`
for the page-by-page record of what each one showed and where it went, so the
decisions are reversible from that document rather than from git history.
"""
